* cl-tuples

** TODO with-tuple-* to (with-tuple (vector3d thing (elements))) ? -- FIXED
** TODO documentation doesn't work! -- FIXED

** TODO refactor clos wrapper to handle situation where accessor-name * 2 != type * 2
** TODO operator overloading
** TODO (:return :element-type ).. in def-tuple-op automatically replaces :element-type with type of tuple

